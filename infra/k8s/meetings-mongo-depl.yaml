apiVersion: apps/v1
kind: Deployment
metadata:
  name: meetings1-mongo-depl
spec:
  replicas: 1
  selector: 
    matchLabels:    
      app: meetings1-mongo 
  template:  
    metadata:  
      labels:   
        app: meetings1-mongo 
    spec: 
      containers:
        - name: meetings1-mongo
          image: mongo

---
apiVersion: v1
kind: Service
metadata:
  name: meetings1-mongo-srv
spec:
  selector:
    app: meetings1-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017


